{
  "name": "labamu-api-client",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "postinstall": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "axios": "^1.0.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.5.1",
    "typescript": "^5.0.0"
  }
}
